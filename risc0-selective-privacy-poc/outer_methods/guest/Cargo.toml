[package]
name = "outer"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "2.2.0", default-features = false, features = ['std'] }
core = {path = "../../core" }
transfer-methods = {path = "../../transfer_methods"}
